# attack_from_mars — observed silenced-OERN sites (all by design)

`WINEDEBUG=warn+vbscript` shows two `Failed ... in resume next mode`
warnings on controller.vbs `LoadController` line 266. Same probe
pattern as american_graffiti — see that example's README for the
full explanation. By design.
